# 🧪 테스트 가이드

이 문서는 Tennis Racket Finder 프로젝트의 테스트 및 검증 절차를 설명합니다.

## 📋 목차

1. [주요 페이지 기능 테스트](#1-주요-페이지-기능-테스트)
2. [필터 조합 테스트](#2-필터-조합-테스트)
3. [리뷰/댓글 작성 테스트](#3-리뷰댓글-작성-테스트)
4. [Rate Limit 동작 확인](#4-rate-limit-동작-확인)
5. [반응형 디자인 검증](#5-반응형-디자인-검증)
6. [번들 크기 분석](#6-번들-크기-분석)
7. [Lighthouse 성능 점수 확인](#7-lighthouse-성능-점수-확인)
8. [접근성 검증](#8-접근성-검증)
9. [크로스 브라우저 테스트](#9-크로스-브라우저-테스트)

---

## 1. 주요 페이지 기능 테스트

### 1.1 랜딩 페이지 (`/`)

**테스트 항목:**

- [ ] 페이지가 정상적으로 로드됨
- [ ] 브랜드 로고 그리드가 표시됨
- [ ] 모든 브랜드가 표시됨 (최소 5개)
- [ ] 브랜드 로고 클릭 시 해당 브랜드 페이지로 이동
- [ ] 중앙 물음표 라켓 컴포넌트가 표시됨
- [ ] "라켓 찾기" 버튼 클릭 시 `/rackets`로 이동
- [ ] 로딩 상태가 정상적으로 표시됨 (초기 로드 시)
- [ ] 에러 발생 시 에러 메시지 표시

**테스트 방법:**

```bash
# 개발 서버 실행
npm run dev

# 브라우저에서 확인
http://localhost:3000
```

**예상 결과:**
- 모든 브랜드가 그리드 형태로 표시됨
- 각 브랜드 로고가 클릭 가능함
- 페이지 로딩 시간이 3초 이내

---

### 1.2 라켓 검색/필터 페이지 (`/rackets`)

**테스트 항목:**

- [ ] 페이지가 정상적으로 로드됨
- [ ] 필터 사이드바가 표시됨 (데스크톱) / 토글 버튼 (모바일)
- [ ] 모든 필터 옵션이 표시됨
- [ ] 라켓 목록이 표시됨
- [ ] 페이지네이션이 정상 작동함
- [ ] 필터 적용 시 URL이 업데이트됨
- [ ] 브라우저 뒤로가기/앞으로가기 시 필터 상태가 유지됨
- [ ] 필터 초기화 버튼이 작동함
- [ ] 빈 결과 시 "검색 결과가 없습니다" 메시지 표시

**테스트 방법:**

```
1. http://localhost:3000/rackets 접속
2. 각 필터를 개별적으로 테스트
3. 여러 필터를 조합하여 테스트
4. URL 복사 후 새 탭에서 열기 (상태 유지 확인)
```

---

### 1.3 브랜드 페이지 (`/brand/[brandSlug]`)

**테스트 항목:**

- [ ] 브랜드 정보 헤더가 표시됨
- [ ] 해당 브랜드의 라켓만 필터링되어 표시됨
- [ ] 브랜드가 존재하지 않으면 404 페이지 표시
- [ ] 브랜드 로고/이미지가 표시됨
- [ ] 라켓 목록이 정상적으로 표시됨

**테스트 방법:**

```
1. http://localhost:3000/brand/wilson 접속
2. 다른 브랜드 슬러그로 테스트 (babolat, head, yonex, prince)
3. 존재하지 않는 브랜드 슬러그로 테스트
```

---

### 1.4 라켓 상세 페이지 (`/racket/[id]`)

**테스트 항목:**

- [ ] 라켓 이미지가 표시됨
- [ ] 라켓 상세 정보가 표시됨
- [ ] 스펙 테이블이 표시됨
- [ ] 평균 평점이 표시됨
- [ ] 리뷰 목록이 표시됨
- [ ] 댓글 목록이 표시됨
- [ ] 조회수가 증가함 (페이지 새로고침 시)
- [ ] 존재하지 않는 라켓 ID는 404 페이지 표시
- [ ] 로딩 상태가 표시됨

**테스트 방법:**

```
1. http://localhost:3000/racket/1 접속
2. 다른 라켓 ID로 테스트
3. 존재하지 않는 ID로 테스트 (404 확인)
4. 조회수 증가 확인 (새로고침 후)
```

---

### 1.5 프로필 페이지 (`/profile`)

**테스트 항목:**

- [ ] 로그인하지 않은 사용자는 로그인 페이지로 리다이렉트
- [ ] 로그인한 사용자는 프로필 정보 표시
- [ ] 내가 작성한 리뷰 목록 표시
- [ ] 내가 작성한 댓글 목록 표시
- [ ] 프로필 수정 폼이 작동함
- [ ] 프로필 이미지 업로드 (Phase 11에서 구현)

**테스트 방법:**

```
1. 로그인하지 않은 상태에서 /profile 접속 (리다이렉트 확인)
2. 로그인 후 /profile 접속
3. 프로필 정보 수정 테스트
4. 리뷰/댓글 목록 확인
```

---

### 1.6 인증 페이지 (`/auth/login`, `/auth/signup`)

**테스트 항목:**

- [ ] 로그인 폼이 정상 작동함
- [ ] 회원가입 폼이 정상 작동함
- [ ] 유효성 검사가 작동함
- [ ] 에러 메시지가 표시됨
- [ ] 성공 시 리다이렉트가 작동함

---

## 2. 필터 조합 테스트

### 2.1 단일 필터 테스트

각 필터를 개별적으로 테스트:

- [ ] **브랜드 필터**: 단일 브랜드 선택
- [ ] **무게 필터**: 최소/최대 값 설정
- [ ] **헤드 사이즈 필터**: 범위 설정
- [ ] **밸런스 필터**: 옵션 선택
- [ ] **스트링 패턴 필터**: 옵션 선택
- [ ] **빔 너비 필터**: 범위 설정
- [ ] **길이 필터**: 범위 설정

### 2.2 복합 필터 테스트

다음 조합들을 테스트:

- [ ] 브랜드 + 무게
- [ ] 브랜드 + 헤드 사이즈 + 밸런스
- [ ] 무게 + 헤드 사이즈 + 스트링 패턴
- [ ] 모든 필터 조합
- [ ] 필터 초기화 후 모든 필터 해제 확인

### 2.3 URL 동기화 테스트

- [ ] 필터 적용 시 URL 쿼리 파라미터 업데이트 확인
- [ ] URL을 복사하여 새 탭에서 열었을 때 필터 상태 유지
- [ ] 브라우저 뒤로가기/앞으로가기 시 필터 상태 유지
- [ ] 북마크 시 필터 상태 저장 확인

**테스트 예시:**

```
1. /rackets?brand=wilson&weight_min=300&weight_max=320 접속
2. 필터가 자동으로 적용되어 있는지 확인
3. 필터를 변경하면 URL이 업데이트되는지 확인
```

---

## 3. 리뷰/댓글 작성 테스트

### 3.1 리뷰 작성

**테스트 시나리오:**

1. **정상 작성**
   - [ ] 로그인 후 라켓 상세 페이지 접속
   - [ ] 리뷰 폼 작성 (평점, 제목, 내용, 선택 사항)
   - [ ] 제출 성공 확인
   - [ ] 리뷰 목록에 새 리뷰 표시
   - [ ] 평균 평점 업데이트 확인

2. **유효성 검사**
   - [ ] 평점 미선택 시 에러 메시지
   - [ ] 내용 10자 미만 시 에러 메시지
   - [ ] 로그인하지 않은 사용자는 리뷰 작성 불가

3. **중복 리뷰 방지**
   - [ ] 같은 라켓에 두 번째 리뷰 작성 시도
   - [ ] 중복 에러 메시지 확인

4. **리뷰 수정/삭제**
   - [ ] 본인 리뷰 수정 가능
   - [ ] 본인 리뷰 삭제 가능
   - [ ] 타인 리뷰 수정/삭제 불가

### 3.2 댓글 작성

**테스트 시나리오:**

1. **정상 작성**
   - [ ] 로그인 후 라켓 상세 페이지 접속
   - [ ] 댓글 작성
   - [ ] 제출 성공 확인
   - [ ] 댓글 목록에 새 댓글 표시

2. **대댓글 작성**
   - [ ] 기존 댓글에 답글 버튼 클릭
   - [ ] 대댓글 작성
   - [ ] 대댓글이 올바른 위치에 표시됨

3. **유효성 검사**
   - [ ] 내용 2자 미만 시 에러 메시지
   - [ ] 로그인하지 않은 사용자는 댓글 작성 불가

4. **댓글 수정/삭제**
   - [ ] 본인 댓글 수정 가능
   - [ ] 본인 댓글 삭제 가능
   - [ ] 타인 댓글 수정/삭제 불가

### 3.3 Rate Limit 테스트

- [ ] 1분에 5번 이상 리뷰 작성 시도 → 429 에러 확인
- [ ] 1분에 10번 이상 댓글 작성 시도 → 429 에러 확인

---

## 4. Rate Limit 동작 확인

### 4.1 테스트 스크립트 실행

```bash
# Rate Limit 테스트 스크립트 실행
npm run test:rate-limit
```

### 4.2 수동 테스트

**리뷰 작성 Rate Limit (1분에 5회):**

```bash
# 빠르게 6번 요청
for i in {1..6}; do
  curl -X POST http://localhost:3000/api/reviews \
    -H "Content-Type: application/json" \
    -d '{"racket_id":1,"rating":5,"content":"테스트 리뷰입니다"}'
  echo ""
done
```

**예상 결과:**
- 처음 5번: 200 OK 또는 401 (인증 필요)
- 6번째: 429 Too Many Requests

**댓글 작성 Rate Limit (1분에 10회):**

```bash
# 빠르게 11번 요청
for i in {1..11}; do
  curl -X POST http://localhost:3000/api/comments \
    -H "Content-Type: application/json" \
    -d '{"racket_id":1,"content":"테스트 댓글"}'
  echo ""
done
```

---

## 5. 반응형 디자인 검증

### 5.1 화면 크기별 테스트

다음 화면 크기에서 각 페이지 테스트:

- [ ] **모바일**: 375px × 667px (iPhone SE)
- [ ] **모바일**: 390px × 844px (iPhone 12/13)
- [ ] **태블릿**: 768px × 1024px (iPad)
- [ ] **데스크톱**: 1920px × 1080px

### 5.2 각 페이지별 확인 사항

**랜딩 페이지:**
- [ ] 모바일에서 브랜드 그리드가 2열로 표시됨
- [ ] 데스크톱에서 브랜드 그리드가 4-5열로 표시됨

**라켓 검색/필터 페이지:**
- [ ] 모바일에서 필터 사이드바가 드로어로 표시됨
- [ ] 데스크톱에서 필터 사이드바가 항상 표시됨
- [ ] 라켓 카드가 화면 크기에 맞게 조정됨

**라켓 상세 페이지:**
- [ ] 모바일에서 이미지가 전체 너비로 표시됨
- [ ] 데스크톱에서 이미지와 정보가 나란히 표시됨

**프로필 페이지:**
- [ ] 모바일에서 폼 요소가 전체 너비로 표시됨

### 5.3 브라우저 개발자 도구 사용

Chrome DevTools에서:
1. F12 또는 Cmd+Option+I
2. Toggle Device Toolbar (Cmd+Shift+M)
3. 다양한 기기 선택하여 테스트

---

## 6. 번들 크기 분석

### 6.1 빌드 및 분석

```bash
# 프로덕션 빌드
npm run build

# 빌드 후 자동으로 번들 크기 분석 결과가 표시됨
```

### 6.2 번들 크기 확인 항목

- [ ] 전체 번들 크기가 500KB 이하
- [ ] 각 페이지 번들이 200KB 이하
- [ ] 큰 번들이 있으면 코드 스플리팅 확인

### 6.3 최적화 방법

번들 크기가 큰 경우:

1. **동적 임포트 사용**
   ```typescript
   const Component = dynamic(() => import('./Component'));
   ```

2. **이미지 최적화**
   - Next.js Image 컴포넌트 사용
   - WebP 포맷 사용

3. **불필요한 의존성 제거**
   ```bash
   npm run analyze  # 번들 분석 (추후 추가)
   ```

---

## 7. Lighthouse 성능 점수 확인

### 7.1 Lighthouse 실행 방법

1. **Chrome DevTools 사용:**
   - F12 → Lighthouse 탭
   - "Analyze page load" 클릭
   - 모바일/데스크톱 선택
   - "Generate report" 클릭

2. **CLI 사용 (선택):**
   ```bash
   npm install -g lighthouse
   lighthouse http://localhost:3000 --view
   ```

### 7.2 목표 점수

- [ ] **Performance**: 90점 이상
- [ ] **Accessibility**: 95점 이상
- [ ] **Best Practices**: 95점 이상
- [ ] **SEO**: 95점 이상

### 7.3 주요 확인 사항

**Performance:**
- [ ] First Contentful Paint (FCP) < 1.8초
- [ ] Largest Contentful Paint (LCP) < 2.5초
- [ ] Cumulative Layout Shift (CLS) < 0.1
- [ ] Time to Interactive (TTI) < 3.8초

**Accessibility:**
- [ ] 모든 이미지에 alt 텍스트
- [ ] 모든 버튼에 접근 가능한 이름
- [ ] 색상 대비 비율 충족
- [ ] 키보드 네비게이션 가능

**SEO:**
- [ ] 메타 태그 설정됨
- [ ] Open Graph 태그 설정됨
- [ ] 시맨틱 HTML 사용

---

## 8. 접근성 검증

### 8.1 키보드 네비게이션

- [ ] Tab 키로 모든 인터랙티브 요소 접근 가능
- [ ] Enter/Space로 버튼 클릭 가능
- [ ] Esc 키로 모달 닫기 가능
- [ ] 포커스 인디케이터가 명확함

### 8.2 스크린 리더 테스트

- [ ] VoiceOver (macOS) 또는 NVDA (Windows) 사용
- [ ] 모든 콘텐츠가 읽혀짐
- [ ] 버튼과 링크의 목적이 명확함

### 8.3 색상 대비

- [ ] 텍스트와 배경의 대비 비율이 4.5:1 이상 (일반 텍스트)
- [ ] 텍스트와 배경의 대비 비율이 3:1 이상 (큰 텍스트)

**확인 도구:**
- [WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/)

### 8.4 ARIA 속성

- [ ] 필수 ARIA 속성 사용 (aria-label, aria-describedby 등)
- [ ] 역할(role) 속성이 올바르게 설정됨
- [ ] 상태 속성 (aria-expanded, aria-selected 등)이 올바르게 업데이트됨

---

## 9. 크로스 브라우저 테스트

### 9.1 테스트할 브라우저

- [ ] **Chrome** (최신 버전)
- [ ] **Firefox** (최신 버전)
- [ ] **Safari** (최신 버전, macOS)
- [ ] **Edge** (최신 버전)
- [ ] **모바일 Safari** (iOS)
- [ ] **모바일 Chrome** (Android)

### 9.2 각 브라우저별 확인 사항

**기능 확인:**
- [ ] 페이지가 정상적으로 로드됨
- [ ] 모든 인터랙션 작동함
- [ ] 필터 기능 작동함
- [ ] 폼 제출 작동함
- [ ] 이미지가 표시됨

**스타일 확인:**
- [ ] 레이아웃이 올바르게 표시됨
- [ ] 폰트가 올바르게 로드됨
- [ ] 색상이 올바르게 표시됨

### 9.3 테스트 도구

- **BrowserStack** (유료, 추천)
- **Sauce Labs** (유료)
- **LambdaTest** (유료)
- 로컬에서 여러 브라우저 직접 테스트

---

## 📝 테스트 체크리스트 요약

각 테스트 항목을 완료한 후 CHECKLIST.md를 업데이트하세요.

```bash
# 테스트 진행 상황 확인
npm run checklist:progress
```

---

## 🔧 문제 발생 시

테스트 중 문제를 발견한 경우:

1. 문제를 상세히 기록
2. 재현 단계 작성
3. 스크린샷 또는 동영상 첨부
4. 이슈 트래커에 등록 (GitHub Issues 등)

---

**마지막 업데이트**: 2026-01-02

